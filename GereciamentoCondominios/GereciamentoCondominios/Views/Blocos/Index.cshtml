@model IEnumerable<GereciamentoCondominios.Models.Bloco>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Numero)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Taxa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Numero)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Taxa)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Pk_Bloco }) |
            @Html.ActionLink("Details", "Details", new { id=item.Pk_Bloco }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Pk_Bloco })
        </td>
    </tr>
}

</table>
